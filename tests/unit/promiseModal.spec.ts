import { expect } from 'chai';
import PromiseModal from '@/components/promiseModal/promiseModal.vue';

describe('promiseModal.vue', () => {
  // const TodoComponent = mount(App, { propsData: { todo } });

  it('should mount for testing', () => {
    expect(1).equal(1);
  });
});
